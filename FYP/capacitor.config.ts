import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'fyp.app',
  appName: 'fypfyp',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
